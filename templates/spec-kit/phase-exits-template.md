# Phase Exit Gate Log: [FEATURE NAME]

Append-only. One row per phase invocation. Never overwrite prior rows. Binary pass/fail only — 1–5 scoring belongs to terminal `/speckit-confidence` only.

| Phase | Result | Repairs | Note |
|-------|--------|---------|------|
