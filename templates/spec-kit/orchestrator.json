{
  "gate_mode": "automatic",
  "model_profile": "balanced",
  "allow_repo_commands": true,
  "implement_hooks": [
    "uv run ruff check",
    "doppler run -- uv run python -m pytest tests/ -x -q"
  ]
}
