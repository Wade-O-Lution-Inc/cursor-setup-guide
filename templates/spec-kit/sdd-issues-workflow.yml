# DEPRECATED — use sdd -i issues=true -i stop_at=tasks
# Prefer templates/spec-kit/sdd-workflow.yml → .specify/workflows/sdd/workflow.yml
# Keep this file only for historical links; do not install as a live workflow.
#
# Equivalent:
#   specify workflow run sdd -i spec="..." -i integration=cursor-agent \
#     # plus the flags implied by the old ID (see docs/sdd-user-guide.md)
